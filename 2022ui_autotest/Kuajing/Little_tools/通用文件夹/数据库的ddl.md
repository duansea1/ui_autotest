<!-- T_EXCHANGE_BATCH_ORDER客兑订单表 -->

`T_EXCHANGE_BATCH_ORDER` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `BATCH_NO` varchar(64) NOT NULL COMMENT '汇兑批次号',
  `CCY_PAIR` varchar(8) NOT NULL COMMENT '货币对',
  `TRADE_DIRECTION` tinyint(4) NOT NULL COMMENT '交易方向，1：买入，2：卖出',
  `SELL_CCY` varchar(3) NOT NULL COMMENT '客户卖出币种',
  `SELL_AMT` decimal(19,2) NOT NULL COMMENT '客户卖出金额',
  `BUY_CCY` varchar(3) NOT NULL COMMENT '客户买入币种',
  `BUY_AMT` decimal(19,2) NOT NULL COMMENT '客户买入金额',
  `RATE` decimal(19,8) NOT NULL COMMENT '成本汇率',
  `CLOSING_DATE` varchar(10) NOT NULL COMMENT '交割日 yyyy-MM-dd',
  `SYS_SELL_CCY` varchar(3) DEFAULT '' COMMENT 'GEP卖出币种',
  `SYS_SELL_AMT` decimal(19,2) DEFAULT NULL COMMENT 'GEP卖出金额',
  `SYS_BUY_CCY` varchar(3) DEFAULT NULL COMMENT 'GEP买入币种',
  `SYS_BUY_AMT` decimal(19,2) DEFAULT NULL COMMENT 'GEP买入金额',
  `CHANNEL_RATE` decimal(19,8) DEFAULT NULL COMMENT '渠道成交汇率',
  `TRADE_CCY` varchar(3) NOT NULL COMMENT '交易币种',
  `AMT_DIRECTION` tinyint(4) NOT NULL COMMENT '金额方向，1：买入，2：卖出',
  `PENDING_AMT` decimal(19,2) DEFAULT NULL COMMENT '待交易金额',
  `PROCESS_AMT` decimal(19,2) DEFAULT NULL COMMENT '处理中金额',
  `USING_AMT` decimal(19,2) DEFAULT NULL COMMENT '已下单金额',
  `PROFIT_CCY` varchar(3) DEFAULT NULL COMMENT '损益币种',
  `PROFIT` decimal(19,2) DEFAULT NULL COMMENT '损益',
  `PROFIT_PERCENT` decimal(19,2) DEFAULT NULL COMMENT '损益比%',
  `SALE_PROFIT` decimal(19,2) DEFAULT NULL COMMENT '销售损益',
  `TRADE_PROFIT` decimal(19,2) DEFAULT NULL COMMENT '交易员损益',
  `TRADE_SOURCE` varchar(10) DEFAULT '' COMMENT '订单来源 MANUAL-人工 AUTO-自动策略',
  `STATUS` varchar(20) NOT NULL COMMENT '状态：INIT待下单、PARTIAL部分已下单、PROCESS处理中、COMPLETED已完成、CANCEL已取消、NOT_PROCESS无需处理',
  `COMPLETE_DATE` timestamp NULL DEFAULT NULL COMMENT '完成时间',
  `CALCULATION_METHOD` tinyint(2) DEFAULT '1' COMMENT '计算方式:1-成本还原法.2-加权平均法',
  `CHILD_DIRECTION_FLAG` tinyint(2) DEFAULT NULL COMMENT '明细交易方向标识：1-单向，2-双向',
  `VERSION` int(11) NOT NULL DEFAULT '1' COMMENT '版本',
  `CREATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `CREATE_BY` varchar(64) NOT NULL,
  `UPDATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `UPDATE_BY` varchar(64) NOT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE KEY `UIDX_BATCH_NO` (`BATCH_NO`) COMMENT '批次号唯一',
  KEY `IDX_CREAT_AT` (`CREATE_AT`) USING BTREE
) ENGINE=InnoDB AUTO_INCREMENT=49 DEFAULT CHARSET=utf8 COMMENT='汇兑批次交易表';


<!-- 渠道货币兑换订单表-T_CURRENCY_EXCHANGE_ORDER -->
`T_CURRENCY_EXCHANGE_ORDER` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `ORDER_NO` varchar(32) DEFAULT NULL COMMENT '订单号',
  `CHANNEL_ID` varchar(128) NOT NULL COMMENT '货币兑换渠道ID',
  `SOURCE_CURRENCY` varchar(8) NOT NULL COMMENT '源币种',
  `TARGET_CURRENCY` varchar(8) NOT NULL COMMENT '目标币种',
  `TRADE_DIRECTION` tinyint(4) NOT NULL COMMENT '交易方向，1：买入，2：卖出',
  `USER_SALE_AMOUNT` decimal(19,2) NOT NULL DEFAULT '0.00' COMMENT '客户卖出金额',
  `USER_BUY_AMOUNT` decimal(19,2) NOT NULL COMMENT '客户买入金额',
  `USER_RATE` decimal(19,6) DEFAULT '0.000000' COMMENT '用户成交汇率',
  `SALE_AMOUNT` decimal(19,2) NOT NULL COMMENT '卖出金额',
  `BUY_AMOUNT` decimal(19,2) NOT NULL COMMENT '买入金额',
  `CHANNEL_TRADE_DIRECTION` tinyint(2) NOT NULL COMMENT '渠道交易方向，1：买入，2：卖出',
  `DEAL_RATE` decimal(19,6) NOT NULL COMMENT '成交汇率',
  `STATUS` tinyint(4) NOT NULL DEFAULT '1' COMMENT '订单状态，下单中5 下单失败8，待交割6，交割处理中11，已交割2',
  `CLOSING_TYPE` varchar(16) DEFAULT NULL COMMENT '交割模式',
  `CLOSING_DATE` varchar(16) DEFAULT NULL COMMENT '交割日期',
  `TRADE_DATE` date NOT NULL COMMENT '交易日期',
  `TRADE_TIME` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '交易时间',
  `CUR_VERSION` int(11) NOT NULL DEFAULT '1' COMMENT '当前记录版本号',
  `PROFIT_AMT` decimal(19,2) DEFAULT NULL COMMENT '损益金额',
  `PROFIT_CCY` char(3) DEFAULT NULL COMMENT '损益币种',
  `PROFIT_RATIO` decimal(19,4) DEFAULT NULL COMMENT '损益百分比',
  `DIFFERENCE_AMT` decimal(19,2) DEFAULT NULL COMMENT '差异金额',
  `CREATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `CREATE_BY` varchar(64) NOT NULL,
  `UPDATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `UPDATE_BY` varchar(64) NOT NULL,
  `SPLIT_FLAG` tinyint(2) DEFAULT '0' COMMENT '是否拆分标识:0-否，1-是',
  `PID` bigint(20) DEFAULT NULL COMMENT '渠道订单父id',
  `CALCULATION_METHOD` tinyint(2) DEFAULT '1' COMMENT '计算方式:1-成本还原法.2-加权平均法',
  `CHILD_DIRECTION_FLAG` tinyint(2) DEFAULT NULL COMMENT '明细交易方向标识：1-单向，2-双向',
  `TRADE_REVIEW_TIME` timestamp NULL DEFAULT NULL COMMENT '复审完成时间',
  `TRADE_MODEL` tinyint(4) NOT NULL DEFAULT '0' COMMENT '下单方式：0-线下，1-API',
  `TRADE_ID` varchar(64) DEFAULT NULL COMMENT '渠道交易id',
  `CHANNEL_REMARK` varchar(255) DEFAULT NULL COMMENT '渠道交易返回描述',
  `CHANNEL_REQUEST_NO` varchar(20) DEFAULT NULL COMMENT '渠道请求流水号',
  `CHANNEL_CLOSING_DATE` date DEFAULT NULL COMMENT '渠道交割日期',
  `PURPOSE` varchar(64) DEFAULT NULL COMMENT '用途-备注见字典EXCHANGE_ORDER_PURPOSE',
  `REMARK` varchar(255) DEFAULT NULL COMMENT '备注信息',
  `PAYMENT_STATUS` tinyint(4) NOT NULL DEFAULT '0' COMMENT '付款到渠道状态，0-待付款、1-付款中、2-付款成功、3-无需付款',
  `SETTLE_STATUS` tinyint(4) NOT NULL DEFAULT '0' COMMENT '渠道结算状态，0-待结算、1-结算中、2-结算成功、3-无需结算',
  `RELATION_PAYMENT_NO` varchar(32) DEFAULT NULL COMMENT '关联付款到渠道订单号',
  `RELATION_SETTLE_NO` varchar(32) DEFAULT NULL COMMENT '关联渠道结算订单号',
  `ORDER_TYPE` tinyint(4) DEFAULT NULL COMMENT '订单类型 1、客户订单 2、自营订单',
  `EXCHANGE_ORDER_TYPE` varchar(16) DEFAULT NULL COMMENT '货币兑换单下单类型 询价汇率-INQUIRY、MARKET汇率-MARKET',
  `AUTO_TYPE` varchar(16) DEFAULT 'MANUAL' COMMENT '是否自动下单 AUTO,MANUAL',
  `CHANNEL_RECONCILIATION_NO` varchar(128) DEFAULT NULL COMMENT '渠道对账流水',
  `DEAL_STATUS` varchar(32) DEFAULT NULL COMMENT '报价单下单状态 INIT 待下单,PROCESSING 下单处理中 ， SUCCESS下单成功 ，FAIL下单失败,字段预留 ',
  `ERROR_CODE` varchar(64) DEFAULT NULL COMMENT '换汇下单错误码（我方）',
  `ERROR_CN_MSG` varchar(512) DEFAULT NULL COMMENT '换汇下单错误描述（我方）',
  `CHANNEL_ERR_CODE` varchar(64) DEFAULT NULL COMMENT '换汇下单渠道错误码 （渠道方）',
  `CHANNEL_ERR_MSG` varchar(512) DEFAULT NULL COMMENT '换汇下单渠道错误描述（渠道方）',
  `BATCH_NO` varchar(64) NOT NULL DEFAULT '' COMMENT '批次订单号',
  `SETTLE_MODE` varchar(32) DEFAULT NULL COMMENT '下单模式 QUOTA-额度交易 BALANCE-余额交易',
  PRIMARY KEY (`ID`),
  UNIQUE KEY `uidx_order_no` (`ORDER_NO`) USING BTREE,
  KEY `idx_create_channel_state` (`TRADE_TIME`,`CHANNEL_ID`,`STATUS`) USING BTREE,
  KEY `idx_create_source_target_ccy` (`TRADE_TIME`,`SOURCE_CURRENCY`,`TARGET_CURRENCY`,`TRADE_DIRECTION`) USING BTREE,
  KEY `idx_trade_id` (`TRADE_ID`) COMMENT '交易id索引'
) ENGINE=InnoDB AUTO_INCREMENT=3015 DEFAULT CHARSET=utf8 COMMENT='货币兑换订单';


<!-- T_CHANNEL_EXCHANGE_PRICE-渠道订单表 -->
`T_CHANNEL_EXCHANGE_PRICE` (
  `ID` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `ORDER_ID` varchar(32) DEFAULT NULL COMMENT '平台订单号',
  `CHANNEL_ID` bigint(20) NOT NULL COMMENT '渠道编号',
  `BUY_CCY` varchar(6) DEFAULT NULL COMMENT '买入货币',
  `CELL_CCY` varchar(6) DEFAULT NULL COMMENT '卖出货币',
  `CURR_CCY` varchar(6) DEFAULT NULL COMMENT '交易货币',
  `CURR_AMOUNT` decimal(19,10) DEFAULT NULL COMMENT '交易金额',
  `SIDE` varchar(8) DEFAULT NULL COMMENT '交易货币的交易方向:卖出: sell 、买入: buy',
  `SETTLE_DAY` date DEFAULT NULL COMMENT '交割日( Y-m-d )',
  `SETTLE_TYPE` varchar(64) DEFAULT NULL COMMENT '交割方式',
  `TRADE_ID` varchar(64) NOT NULL DEFAULT '' COMMENT '交易ID',
  `PRICE` decimal(19,10) DEFAULT NULL COMMENT '价格',
  `PRICED_AT` date DEFAULT NULL COMMENT '报价时间',
  `PRICE_TIMES` int(11) DEFAULT NULL COMMENT '报价有效时间(秒)',
  `AMOUNT` decimal(19,10) DEFAULT NULL COMMENT '交易货币金额',
  `AMOUNT2` decimal(19,10) DEFAULT NULL COMMENT '对应货币金额',
  `STATUS` tinyint(2) DEFAULT NULL COMMENT '状态：-1-处理中、0-初始、1-待交割、2-已交割、3-已拒绝、4-已取消',
  `CHANNEL_ORDER_ID` varchar(64) DEFAULT NULL COMMENT '渠道订单号',
  `REMARKS` varchar(256) DEFAULT NULL COMMENT '备注',
  `BANK_REFERENCE` varchar(64) DEFAULT '',
  `CREATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建人',
  `CREATE_BY` varchar(64) NOT NULL DEFAULT '' COMMENT '创建时间',
  `UPDATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新人',
  `UPDATE_BY` varchar(64) NOT NULL DEFAULT '' COMMENT '更新时间',
  `EXCHANGE_ORDER_NO` varchar(32) DEFAULT NULL COMMENT '货币兑换订单号（冗余）',
  `DEAL_STATUS` varchar(32) DEFAULT 'INIT' COMMENT '报价单下单状态 INIT 待下单,PROCESSING 下单处理中 ， SUCCESS下单成功 ，FAIL下单失败 ',
  `ERROR_CODE` varchar(64) DEFAULT NULL COMMENT '换汇下单错误码（我方）',
  `ERROR_CN_MSG` varchar(512) DEFAULT NULL COMMENT '换汇下单错误描述（我方）',
  `CHANNEL_ERR_CODE` varchar(64) DEFAULT NULL COMMENT '换汇下单渠道错误码 （渠道方）',
  `CHANNEL_ERR_MSG` varchar(512) DEFAULT NULL COMMENT '换汇下单渠道错误描述（渠道方）',
  PRIMARY KEY (`ID`),
  UNIQUE KEY `UK_CID_TID` (`CHANNEL_ID`,`TRADE_ID`)
) ENGINE=InnoDB AUTO_INCREMENT=2838 DEFAULT CHARSET=utf8mb4;







<!-- T_TRADE_RECEIPT -用户兑换凭证表 -->
`T_TRADE_RECEIPT` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `USER_NO` varchar(64) NOT NULL COMMENT '客户号',
  `TRADE_TYPE` tinyint(4) NOT NULL COMMENT '交易类型，1：付款，2：提现，3：货币兑换',
  `RECEIPT_NO` varchar(32) NOT NULL COMMENT '交易订单号',
  `REQUEST_NO` varchar(32) NOT NULL COMMENT '请求单号',
  `ORIGINAL_SALE_CURRENCY` varchar(3) DEFAULT NULL COMMENT '原始币种',
  `ORIGINAL_SALE_AMOUNT` decimal(19,2) DEFAULT NULL COMMENT '原始金额',
  `SOURCE_CURRENCY` varchar(8) NOT NULL COMMENT '源币种',
  `TARGET_CURRENCY` varchar(255) NOT NULL COMMENT '目标币种',
  `TRADE_DIRECTION` tinyint(4) NOT NULL COMMENT '交易方向，1：买入，2：卖出',
  `EXCHANGE_RATE` decimal(19,4) NOT NULL COMMENT '汇率',
  `SALE_AMOUNT` decimal(19,2) NOT NULL COMMENT '卖出金额',
  `BUY_AMOUNT` decimal(19,2) NOT NULL COMMENT '买入金额',
  `TRADE_DATE` date NOT NULL COMMENT '交易日期',
  `TRADE_TIME` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '交易时间',
  `STATUS` tinyint(4) NOT NULL DEFAULT '1' COMMENT '关联订单交易状态，1：处理中，2：已完成',
  `CLOSING_TYPE` varchar(16) DEFAULT NULL COMMENT '交割模式',
  `CLOSING_DATE` varchar(16) DEFAULT NULL COMMENT '交割日期',
  `CUR_VERSION` tinyint(4) NOT NULL DEFAULT '1' COMMENT '当前记录的版本号',
  `CHANNEL_ORDER_NO` varchar(32) DEFAULT NULL COMMENT '渠道货币兑换批次单号',
  `TRADE_STATUS` tinyint(2) NOT NULL COMMENT '渠道交易状态：1-待交易、2-处理中、3-交易完成',
  `PROFIT_AMT` decimal(19,2) DEFAULT NULL COMMENT '损益金额',
  `SALES_PROFIT_AMT` decimal(19,2) DEFAULT '0.00' COMMENT '销售损益金额',
  `TRADES_PROFIT_AMT` decimal(19,2) DEFAULT '0.00' COMMENT '交易员损益金额',
  `PROFIT_CCY` char(3) DEFAULT NULL COMMENT '损益币种',
  `PROFIT_RATIO` decimal(19,4) DEFAULT NULL COMMENT '损益比率',
  `ORDER_GEP_RATE` decimal(19,6) DEFAULT NULL COMMENT '关联订单第三方汇率',
  `ORDER_CREATE_AT` timestamp NULL DEFAULT NULL COMMENT '关联订单创建时间',
  `ORDER_AGENT_RATE` decimal(19,6) DEFAULT NULL COMMENT '关联订单代理商汇率',
  `RECEIPT_GEP_RATE` decimal(19,6) DEFAULT NULL COMMENT '凭证第三方汇率',
  `RECEIPT_SOURCE_GEP_RATE` decimal(19,6) DEFAULT NULL COMMENT '凭证原货币对第三方汇率',
  `ORIGINAL_CCY_PAIR` varchar(8) DEFAULT NULL COMMENT '原订单货币对',
  `ORIGINAL_TRADE_DIRECTION` tinyint(2) DEFAULT NULL COMMENT '原订单交易方向',
  `CREATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `CREATE_BY` varchar(64) NOT NULL,
  `UPDATE_AT` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `UPDATE_BY` varchar(64) NOT NULL,
  `RATE_ID` varchar(64) DEFAULT NULL COMMENT '汇率日志ID',
  `PAYMENT_TYPE` tinyint(2) DEFAULT NULL COMMENT '付款类型：1-提现，2-付款，3-GEP境内结汇，4-收款账户用户提现，5-收款账户用户付款，6-收款账户结汇，7-GEP境内垫资结汇，8-B2B付款，9-b2b结汇',
  `BIZ_TYPE` tinyint(2) DEFAULT NULL COMMENT '汇率查询使用业务类型：0-全局，5-vcc，7-gep汇兑，8-电商服贸，9-b2b',
  `CHANNEL_CODE` varchar(32) DEFAULT NULL COMMENT '交易标签',
  `TRADE_BUY_AMOUNT` decimal(19,2) DEFAULT NULL COMMENT '原始买入金额',
  `TRADE_SALE_AMOUNT` decimal(19,2) DEFAULT NULL COMMENT '原始卖出金额',
  PRIMARY KEY (`ID`),
  KEY `uidx_request_no_1` (`REQUEST_NO`,`TRADE_TYPE`),
  KEY `T_TRADE_RECEIPT` (`CREATE_AT`,`STATUS`) USING BTREE,
  KEY `idx_trade_customer` (`TRADE_TIME`,`USER_NO`) USING BTREE,
  KEY `idx_channel_order` (`CHANNEL_ORDER_NO`) USING BTREE
) ENGINE=InnoDB AUTO_INCREMENT=38521 DEFAULT CHARSET=utf8 COMMENT='交易凭证';